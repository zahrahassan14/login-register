// const moment = require('moment');

// const generateDailyPassword = (basePassword) => {
//     const today = moment().format('MMDDYYYY');
//     return `${basePassword}${today}`;
// };

// module.exports = { generateDailyPassword };
